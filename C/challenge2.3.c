/* Challenge answers:
    1. Char is a character type (i.e. part of a string), int is an actual number, for use in calculations, etc.
    2. Char when working with strings (or coerce from int), int for any calculation.
    3...
*/
#include <stdio.h>

int main() {
    int one = 1;
    int two = 2;

    int result = one + two;

    printf("One plus two equals: %d\n", result);
}
